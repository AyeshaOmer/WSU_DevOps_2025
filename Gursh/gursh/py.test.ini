@'
[pytest]
# Only run our test folder
testpaths = gursh/modules/tests

# Don’t recurse into build/venv output
norecursedirs = cdk.out .venv venv node_modules dist build

# Make the project root importable: "import gursh" works
pythonpath = .
'@ | Out-File -Encoding UTF8 -FilePath .\pytest.ini
